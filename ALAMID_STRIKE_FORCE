import os, smtplib, telebot, sys, time
from email.mime.text import MIMEText
from email.header import Header

# --- الإعدادات ---
TOKEN = "8717546599:AAHgnXRdPwoz1mV85LZft962ZU19xXnHBks"
MY_EMAIL = "oedn305@gmail.com"
CHAT_ID = "6381014522"
raw_pass = str(os.getenv("GMAIL_PASS", "")).strip()
MY_PASS = "".join(filter(lambda x: x.isascii() and not x.isspace(), raw_pass))

bot = telebot.TeleBot(TOKEN)

# --- محرك الرد القانوني (تعديل مشعل: رد كامل ومسند للأنظمة) ---
@bot.message_handler(func=lambda message: True)
def legal_expert_system(message):
    user_query = message.text
    print(f"📥 استفسار جديد: {user_query}")
    
    # رسالة انتظار احترافية
    bot.send_chat_action(message.chat.id, 'typing')

    if any(word in user_query for word in ["سارق", "سرقة", "سرق"]):
        response = (
            "⚖️ **الرأي القانوني الرسمي (جريمة السرقة)**\n\n"
            "بناءً على **نظام الإجراءات الجزائية** والأنظمة المحدثة من وزارة العدل:\n\n"
            "1️⃣ **التوصيف النظامي:** تعتبر جريمة السرقة من الجرائم الموجبة للإيقاف وفق القرار الوزاري الصادر بناءً على المادة (112) من نظام الإجراءات الجزائية.\n"
            "2️⃣ **العقوبة:** تخضع العقوبة لتقدير المحكمة الجزائية (تعزيراً) بالسجن والتعويض، ما لم تكتمل شروط الحد الشرعية التي ينظر فيها القضاء.\n"
            "3️⃣ **الجهة المختصة:** النيابة العامة (للتحقيق والادعاء)، والمحكمة الجزائية (لإصدار الحكم).\n\n"
            "📍 **توجيه المكتب:** ننصح بتقديم بلاغ رسمي عبر تطبيق (كلنا أمن) أو مراجعة أقرب مركز شرطة لإثبات الواقعة.\n\n"
            "✅ **المستند:** الأنظمة العدلية السعودية 2026."
        )
    else:
        response = (
            "⚖️ **مكتب المستشار القانوني السعودي**\n\n"
            f"بخصوص طلبكم: ({user_query})\n\n"
            "🔹 **التحليل:** جاري مطابقة الحالة مع مواد (نظام المرافعات) أو (نظام العمل) حسب الاختصاص.\n"
            "🔹 **الإجراء:** يمكنك تصعيد الطلب عبر بوابة **(ناجز)** التابعة لوزارة العدل.\n\n"
            "📩 سيقوم المستشار المختص بتزويدك بالمواد التفصيلية حال اكتمال المراجعة."
        )

    bot.reply_to(message, response, parse_mode="Markdown")

def start_alamid():
    # 1. حل مشكلة الـ Conflict (طرد النسخ القديمة)
    print("🧹 جاري تنظيف الجلسات القديمة...")
    try:
        bot.remove_webhook()
        time.sleep(1)
        # سحب آخر التحديثات لتصفير العداد
        bot.get_updates(offset=-1)
    except: pass

    print("🚀 [نظام العميد] متصل الآن وجاهز للرد...")
    try:
        bot.send_message(CHAT_ID, "✅ تم تشغيل نظام المستشار بنجاح. البوت يرد الآن بالأنظمة الوزارية.")
    except: pass
    
    # 2. تشغيل الاستماع المستمر
    bot.infinity_polling(timeout=20, long_polling_timeout=10)

if __name__ == "__main__":
    start_alamid()
