import os, smtplib, telebot, sys, time
from email.mime.text import MIMEText
from email.header import Header

# --- الإعدادات ---
TOKEN = "8717546599:AAHgnXRdPwoz1mV85LZft962ZU19xXnHBks"
MY_EMAIL = "oedn305@gmail.com"
CHAT_ID = "6381014522"
raw_pass = str(os.getenv("GMAIL_PASS", "")).strip()
MY_PASS = "".join(filter(lambda x: x.isascii() and not x.isspace(), raw_pass))

# إنشاء البوت
bot = telebot.TeleBot(TOKEN)

# --- محرك الرد القانوني بالمواد الوزارية ---
@bot.message_handler(func=lambda message: True)
def legal_ai_reply(message):
    query = message.text
    print(f"📥 طلب استشارة: {query}")
    
    # تحديد الرد بناءً على الأنظمة السعودية
    if any(word in query for word in ["سارق", "سرقة", "اسرق"]):
        response = (
            "⚖️ *الرد القانوني الرسمي (جريمة السرقة)*\n\n"
            "بناءً على *نظام الإجراءات الجزائية* و*نظام العقوبات* الصادر عن وزارة العدل:\n\n"
            "🔹 *التوصيف:* تعتبر فعل جرمي يستوجب الإيقاف وفق المادة (112) من نظام الإجراءات.\n"
            "🔹 *العقوبة:* تعزيرية يحددها القاضي بناءً على حيثيات القضية (سجن، غرامة، أو كليهما).\n"
            "🔹 *الجهة المختصة:* النيابة العامة للتحقيق، والمحكمة الجزائية للتقاضي.\n\n"
            "📍 *ملاحظة:* إذا كانت السرقة من مكان محكم (حرز) قد تتحول لمطالبة بحد السرقة عند اكتمال الشروط الشرعية.\n\n"
            "✅ *المصدر:* بوابة الأنظمة السعودية - وزارة العدل."
        )
    else:
        response = (
            "⚖️ *نظام المستشار القانوني السعودي*\n\n"
            f"بخصوص استفساركم عن: ({query})\n\n"
            "تم توجيه الطلب للمراجعة النظامية. للإحاطة:\n"
            "1. يتم العمل وفق *نظام المرافعات الشرعية*.\n"
            "2. يمكنك متابعة طلبك عبر منصة *ناجز* الرسمية.\n\n"
            "📩 سيتم تزويدك بالمواد النظامية التفصيلية حال اكتمال المراجعة."
        )
    
    bot.reply_to(message, response, parse_mode="Markdown")

def start_system():
    # الخطوة الأهم: حذف أي Webhook أو جلسة قديمة لمنع خطأ الـ Conflict
    print("🧹 جاري تنظيف الجلسات القديمة ومنع التضارب...")
    bot.remove_webhook()
    time.sleep(2) # انتظار بسيط للتأكد من خروج النسخة القديمة

    print("🚀 [نظام العميد] بدأ الآن بدون أخطاء...")
    try:
        bot.send_message(CHAT_ID, "⚖️ نظام المستشار السعودي متصل الآن وجاهز للرد بالأنظمة.")
    except: pass
    
    # تشغيل البوت مع خاصية التجاوز
    bot.infinity_polling(timeout=20, long_polling_timeout=10)

if __name__ == "__main__":
    start_system()
